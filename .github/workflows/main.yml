on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - uses: actions/checkout@v1
      - uses: ./
        with:
          token: '${{ secrets.SOMETHING_SECRET }}'
